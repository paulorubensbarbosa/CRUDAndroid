<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.activity.activity.DetalhesActivity"></activity>
        <activity android:name=".activity.activity.activity.CadastroActivity" />
        <activity android:name=".activity.activity.activity.SobreActivity" />
        <activity android:name=".activity.activity.activity.SocialActivity" />
        <activity android:name=".activity.activity.activity.ListaActivity" />
        <activity android:name=".activity.activity.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>